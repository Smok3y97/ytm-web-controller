# AI Collaboration & Transparency Disclosure (`docs/ai-disclosure.md`)

Full transparency and open-source honesty are core principles of the **YouTube Music Web Controller** project. This document transparently explains the development methodology, the extent of AI-generated code, and the hardware verification process.

---

## 🤖 1. 100% AI-Generated Codebase

**100% of the source code, build scripts, vector assets, and UI components in this repository were generated by Artificial Intelligence** using **Google Antigravity** (powered by Google DeepMind's advanced reasoning models).

```
┌────────────────────────────────────────────────────────┐
│               Human Maintainer (Smok3y97)              │
│                                                        │
│ • Defines features, design goals, and UX requirements  │
│ • Directs architecture refactoring and modularization  │
│ • Conducts comprehensive manual hardware testing & QA  │
└───────────────────────────┬────────────────────────────┘
                            │ Real-Time Interactive
                            │ Prompting & Iteration
                            ▼
┌────────────────────────────────────────────────────────┐
│             Google Antigravity / Gemini AI             │
│                                                        │
│ • Generates all TypeScript plugin source code          │
│ • Generates Manifest V3 browser extension scripts      │
│ • Generates PowerShell packaging & build automation    │
│ • Computes SVG vector paths & CSS dark theme styling   │
│ • Writes technical documentation & architecture specs  │
└────────────────────────────────────────────────────────┘
```

---

## 🛠️ 2. What the AI Created

1. **Stream Deck Plugin Backend (TypeScript / SDK 2)**:
   - Full implementation of decoupled backend services (`websocket-server.ts`, `state-manager.ts`, `marquee-service.ts`, `image-renderer.ts`, `discord-rpc.ts`, `obs-exporter.ts`, `clipboard.ts`).
   - Action controllers for dual-state keys, step volume adjustments, and dials.
2. **Browser Companion Extension (Manifest V3)**:
   - Zero-polling HTML5 `<video>` event hooks and DOM `MutationObserver` state extractions in `content.js`.
   - Dynamic port storage and live health check popup UI in `popup/`.
3. **Property Inspector (PI) Frontend**:
   - Decoupled WebSocket SDK client bridge (`streamdeck-client.js`) and modular Global Settings UI component (`global-settings.js`).
   - Pure Vanilla CSS (`sdpi.css`) dark theme design system.
4. **Vector SVGs & Build Automation**:
   - Algorithmic SVG vector generation script (`generate_assets.ps1`) and one-click packaging pipeline (`package_plugin.ps1`).

---

## 🧪 3. Hardware Testing & Manual Verification by Maintainer

While the code is AI-generated, **all features, UI layouts, dials, and integrations undergo extensive physical testing and quality verification by the maintainer (Smok3y97)**.

### Verified Hardware & Environment Specifications:

- **Operating System**: Windows 11
- **Hardware Controllers**:
  - **Elgato Stream Deck +** (Physical Keys, Dials & LCD Touchstrip)
  - **Corsair Galleon 100 SD** (Physical Keys & Dials)
- **Software Tested**:
  - **Elgato Stream Deck Software**: `v7.5.1 (22901)` (Minimum SDK 2 requirement: `v6.5+`)
  - **Discord Windows Client**: `v1.0.9253 x64 (88414)` (Desktop Rich Presence)
  - **OBS Studio**: `v28+` (Text GDI+ stream overlays)
  - **Browser**: Google Chrome (Official Web Player & PWA)

---

## 🔍 4. Open-Source Transparency & Dependencies Note

- **No Manual Dependency Auditing**: The maintainer guides the AI and physically tests the resulting plugin on actual hardware, but does **not** conduct independent manual line-by-line audits of npm dependency packages.
- **Automated Validation**: Every release build is automatically validated against the official Elgato SDK schema using `npx streamdeck validate` with zero warnings and zero errors.
- **Community Review**: The entire codebase is completely open-source under the **MIT License**. Review, issues, and pull requests from the community are warmly welcomed!
